# README

A basic Ruby on Rails app that demonstrates CRUD capabilities. Users can create an account, sign in/out, add contacts, edit contacts, view contacts, and delete contacts, with contacts having different associations so that only a specific user's contacts are visible when logged in.

Note: There is a bug associated with signing out and deleting contacts
TODO: Host on Heroku
